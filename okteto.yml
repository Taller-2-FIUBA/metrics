deploy:
  - envsubst < kubernetes/configmap.yaml > kubernetes/configmap.yaml
  - kubectl apply -f kubernetes/configmap.yaml
  - kubectl apply -f kubernetes/deployment.yaml
  - kubectl apply -f kubernetes/service.yaml
